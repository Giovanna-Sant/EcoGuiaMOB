import React from "react";
import { View, Text, ScrollView } from "react-native"

const Faq = () => {
    return (
        <ScrollView>
            <Text>FAQ Page</Text>
        </ScrollView>
    )
}

export default Faq