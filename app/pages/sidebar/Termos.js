import React from "react";
import { View, Text, ScrollView } from "react-native"

const Termos = () => {
    return (
        <ScrollView>
            <Text>Termos Page</Text>
        </ScrollView>
    )
}

export default Termos